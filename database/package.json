{
  "name": "@aegis/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "migrate:reset": "prisma migrate reset --force",
    "seed": "tsx prisma/seeds/seed.ts",
    "studio": "prisma studio",
    "reset": "prisma migrate reset --force && tsx prisma/seeds/seed.ts",
    "build": "echo 'No build step for database'",
    "lint": "echo 'No lint for database'",
    "typecheck": "echo 'No typecheck for database'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
